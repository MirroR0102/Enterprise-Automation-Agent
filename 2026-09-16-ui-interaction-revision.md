# 修改建议：第 4 部分唯一维护清单（界面 + 数据 + 汇报材料）

> **For agentic workers — 强制执行约定（用户要求）：**
>
> 1. **每次动手前先完整阅读本文件**（含进度表）。
> 2. **跳过所有已标记为 ✅ 已完成 的章节/条目**，只做未完成（☐）与新增（🆕）项。
> 3. **每完成一项**：立刻在本文件把该条改为 `✅ 已完成`，并在下方「进度日志」追加一行：`日期 · 完成：… · 涉及文件：…`。
> 4. 若某条做了一半被打断：标 `🟡 进行中` 并写清卡点，下次从这里继续。
> 5. 不要另建第二份修改建议 md；新需求都追加到本文件。原 `2026-09-16-presentation-upgrade.md` 已并入下文 **P0–P4**，勿再分开维护。
> 6. **未获用户许可勿 `git push`。**
>
> 工作区：桌面 `企业业务流程自动化Agent`。

**Goal:** （1）工作台交互/身份/分库/过往周报等产品能力；（2）汇报材料升级到可念满约 30 分钟、PPT 多图、另附报告执行说明。一次任务可只做表中未完成项的一个子集，但清单只此一份。

---

## 进度总表（每次先看这里）

| ID | 项 | 状态 |
| :--- | :--- | :--- |
| L1 | 左侧能力栏 + 浅色壳（历史） | ✅ 已完成 |
| L2 | 状态中文芯片 + 取消仅 running 可点（基础） | ✅ 已完成 |
| L3 | 底部 composer + 过程主区 + 右侧 Markdown 分屏 | ✅ 已完成（骨架已在；细调可随新需求） |
| M1 | 同会话复用 session +「新开对话」+ 记忆演示话术 | ✅ 已完成 |
| U1 | 发送成功后**清空对话框** | ✅ 已完成 |
| U2 | 左下角：粗线位=**昵称**，细线位=**登录用户名**（去掉「—」） | ✅ 已完成 |
| U3 | 处理 user-card **顶部分割线**（说明或弱化） | ✅ 已完成 |
| U4 | 头像 → 设置：改昵称 + 周报保存目录 | ✅ 已完成 |
| U5 | 执行过程入参/返回**默认展开**（勿默认收起） | ✅ 已完成 |
| DB1 | MySQL 连接 + 每用户分库：会话上下文与过往周报 | ✅ 已完成 |
| U6 | 前端「过往周报」入口（顶栏右侧） | ✅ 已完成 |
| D1 | 文档：上周格式→本周 记忆演示话术写入 README/acceptance | ✅ 已完成 |
| P0 | 汇报：读范本三件套 + 现有 PPT/讲稿，列差距 | ✅ |
| P1 | 汇报：重做 PPT（少字多图，含运行逻辑图等） | ✅ |
| P2 | 汇报：重写讲稿 PDF（~30 分钟，含代码实现段） | ✅ |
| P3 | 汇报：新建《报告执行说明》PDF | ✅ |
| P4 | 汇报：三件套对齐页码/时间轴 + README 链接 | ✅ |

---

## 进度日志

- 2026-09-16 · Agent 完成 M1/U1–U6/DB1/D1：会话复用+新开对话、发送清空、昵称/登录名、设置面板、入参默认展开、每用户分库与过往周报 API/UI、acceptance 记忆话术。涉及：`web/*`、`app/db/user_store.py`、`app/api/{chat,reports}.py`、`tests/test_reports.py`、`README.md`、`AGENT.md`、`scripts/acceptance_check.md`。P0–P4 汇报材料仍未做。顶部分割线为有意弱化分隔（U3）。
- 2026-09-16 · 侧栏身份/设置修复后本地 commit；完成 P0–P4：重写 `scripts/build_presentation.py`，产出 20 页少字多图 PPT、~30′ 讲稿、报告执行说明（md+pdf）；差距见下「P0 差距记录」。
- 2026-09-16 · 用户确认本文件为唯一修改清单；新增 U1–U4；指出左下「横线」实为未填充的 `—` 占位。
- 2026-09-16 · 用户补充：登录名直接填充下行；设置可改昵称；上行粗=昵称，下行浅=登录用户名。
- 2026-09-16 · 用户询问入参默认收起原因；改为 U5：入参/返回默认展开。
- 2026-09-16 · 用户要求 MySQL 每用户分库存对话与过往周报；前端增加过往周报入口（建议顶栏右侧）。
- 2026-09-16 · 用户要求将汇报材料升级（及打包给 Cursor 的说明）并入本文件；废止单独 presentation-upgrade 维护。
- （Agent 每完成一项在此追加）

---

## ✅ 已完成（跳过，勿重做）

### L1–L3 摘要

- 浅色 UI、侧栏「运营周报 Agent / 知识库问答」。
- 周报区：`workspace` 上 + `composer` 下；`split` + `previewPane` 出 final 后右侧预览。
- 状态芯片中文；取消按钮基础 disabled 逻辑已有——若回归再修，勿推倒重来。

---

## ☐ / 🆕 未完成（只做这些）

### M1 · 会话记忆可演示

- [ ] 确认发送任务**不会**每次 `sessionId = ""`；仅「新开对话」才重置。
- [ ] 界面显示当前会话短 id。
- [ ] 推荐演示（写入 README 或 `scripts/acceptance_check.md`）：
  1. 同一会话生成「上周」完整周报；
  2. 追问：`记住刚才上周周报的章节结构和写法。请用同样的格式生成「本周」运营周报 markdown，不要改栏目顺序。`
- [ ] 完成后把本条标 ✅，写进度日志。

### U1 · 发送后清空输入框 🆕

- [ ] 用户点击发送且请求成功受理后，立刻清空 `#prompt`（可保留 placeholder）。
- [ ] 发送失败则不清空，便于重试。
- [ ] 完成后标 ✅。

### U2 · 左下角双行身份（消除「怪横线」）🆕

**现象（用户截图）：** 头像右侧两道横线 +「退出」。  
**原因：** 原 `#userName` / `#userRole` 占位符 **`—`** 看起来像线。  
**用户拍板（2026-09-16）：**

| 位置 | 视觉 | 内容 |
| :--- | :--- | :--- |
| 上面（原粗线位） | 更醒目 / 字重更高 | **昵称**（用户在设置里改；默认可用登录名） |
| 下面（原细线位） | 更浅、更小 | **登录时的用户名**（登录表单提交的 `username`，只读展示） |

- [ ] 登录成功时：用表单里的 `username` 写入 `localStorage`（如已有 `eoa_username`），**必须**渲染到下方「登录名」行，禁止再显示 `—`。
- [ ] 上方「昵称」行：读 `localStorage` 键如 `eoa_nickname`；若空则默认等于登录用户名。
- [ ] 角色「运营/开发」可放到昵称旁小字、头像 title，或设置里展示——**不要**再用第三行 `—`。
- [ ] 刷新后两行都有字，不再出现双横线观感。
- [ ] 完成后标 ✅。

### U3 · user-card 顶部分割线 🆕

- [ ] 保留亦可，但不要让用户以为是 bug：可略减对比度，或改为更短的内边距分隔。
- [ ] 若与 U2 同时修，在进度日志注明「顶部分割线为有意分隔」。
- [ ] 完成后标 ✅。

### U4 · 头像点击 → 设置（改昵称 + 周报保存）🆕

- [ ] 点击头像（或用户卡片主区域）弹出菜单/面板：**设置**、退出。
- [ ] **设置 · 修改名称（昵称）**（用户明确要求）：
  - 可编辑昵称，保存到 `eoa_nickname`；
  - 保存后立刻更新左下**上方粗线位**文案；
  - **下方浅色行仍显示登录用户名**，改昵称不改登录名。
- [ ] **设置 · 周报保存**：
  - `localStorage` 如 `eoa_report_dir` 或「相对 `reports/` 的默认文件名」；
  - 浏览器无法任意写绝对路径时，UI 诚实说明；最小实现保存偏好即可，能接到写文件工具更好。
- [ ] 设置可关闭；浅色壳风格一致。
- [ ] 完成后标 ✅。


### U5 · 执行过程入参/返回默认展开 🆕

**说明：** 此前「用 `<details>` 默认折叠入参」仅为减少字墙的可选建议，**用户不认同作为默认行为**。本项目需要在执行过程里直接看到工具入参，便于答辩展示。

- [ ] 时间线中工具 **入参、返回默认展开可见**，不要默认 `<details closed>` / 默认折叠。
- [ ] 若单条内容过长（例如 >2KB 或 >30 行）：可提供「收起」按钮或仅截断超长部分并显示「展开全部」，但**首屏默认仍应看到入参开头**。
- [ ] 自检：跑一轮带 `mysql_query` / `web_search` 的任务，执行过程里无需点击即可看见入参。
- [ ] 完成后标 ✅。


### DB1 · MySQL 连接与每用户分库（对话上下文 + 过往周报）🆕

**用户意图：** 接通 MySQL；库中为**每位用户建立一个分库**，专门保存该用户的对话上下文，以及曾经生成的周报；前端可访问过往周报。

**推荐实现（答辩可讲清、工程可落地）：**

1. **业务主库**（已有或沿用）：`users` / 全局 `agent_logs`（可选）仍可在主库。
2. **每用户分库命名：** `eoa_u_{user_id}` 或 `eoa_u_{safe_username}`（只含 `[a-zA-Z0-0_]`，防注入）。
   - 用户首次登录或首次发任务时：若不存在则 `CREATE DATABASE`（或 MySQL **Schema** 同等物）。
3. **分库内最少表：**
   - `conversations(id, title, created_at, updated_at)` — 对应前端「会话 / 新开对话」
   - `messages(id, conversation_id, role, content, created_at)` — 用户句与助手摘要/最终回复
   - `tool_events(id, conversation_id, event_json, created_at)` — 可选，存时间线事件便于回放
   - `weekly_reports(id, conversation_id, title, markdown, file_relpath, created_at)` — 每次成功 `final` 周报落库一份
4. **与 MemorySaver 关系：**
   - 进程内 `MemorySaver` 可继续服务「当前进程多轮」；
   - **跨刷新/重启**的上下文与过往周报以分库为准；新开对话 = 新 `conversations` 行。
5. **写周报时：** 文件仍可写 `reports/`（或设置中的相对路径）；同时 **INSERT `weekly_reports`**，保证「过往周报」列表不依赖磁盘扫目录。
6. **`USE_MOCK_DB=true`：** 用 SQLite 模拟「分库」——例如 `data/users/u_{id}.db` 每用户一个文件，表结构同上；README 写明与 MySQL 分库的对应关系。
7. **权限：** 只能读写**当前登录用户**自己的分库；禁止跨用户。
8. **SQL 工具（查销售）** 仍走业务只读库/seed，与「用户分库」分离，避免 Agent 查销售时误入用户上下文库。

**API（最小）：**

- `GET /api/reports` → 当前用户过往周报列表（id, title, created_at）
- `GET /api/reports/{id}` → 单份 markdown
- （可选）`GET /api/conversations` / 消息回放

**实现检查：**

- [ ] `USE_MOCK_DB=false` 时能连上 MySQL（`.env` / README 写清 `MYSQL_DSN`）。
- [ ] 每用户自动建分库+表；写入会话消息与成功周报。
- [ ] 列表/详情 API + 鉴权。
- [ ] mock 模式有对等隔离。
- [ ] 完成后标 ✅ 并写进度日志。

### U6 · 前端「过往周报」按钮位置 🆕

**推荐位置（已与用户说明）：** 周报主区 **顶栏右侧**（`panelTitle` 一行）：`过往周报` | `新开对话` | （开发日志仍可全局）。

- 点击后：右侧滑出列表面板（可复用分屏预览栏模式），列出历史周报；点一项在预览区打开 Markdown。
- **不要**做成与「知识库问答」并列的第三能力（避免削弱第 3/第 4 分工叙事），除非用户以后改口。
- [ ] 实现按钮 + 列表 + 预览；无数据时友好空态。
- [ ] 完成后标 ✅。

### D1 · 文档

- [ ] README 或 acceptance 补上记忆演示两轮话术 + 发送清空、设置项说明。
- [ ] 完成后标 ✅。

---

## 验收标准（全部 ☐/🆕 完成后）

1. 发送成功后输入框清空。
2. 左下角上方为昵称、下方为登录用户名，不再出现「—」双横线观感。
3. 头像可点开设置；可改昵称；有周报保存相关偏好。
4. 同会话可按「上周格式 → 本周」演示记忆；新开对话才清空上下文。
5. 取消仅执行中可点；分屏预览等既有行为不回归。
6. 执行过程中工具入参/返回默认展开可见。
7. MySQL（或 mock 每用户库文件）保存对话与过往周报；顶栏可打开过往周报列表并预览。

---


---

## 汇报材料升级（P0–P4）· 原 presentation-upgrade 已并入

### 交给 Cursor 时建议打包（用户要求写进本文件）

一次做汇报相关任务时，请同时打开/附上：

1. **本文件** `2026-09-16-ui-interaction-revision.md`（只做其中 **P0–P4** 未完成项即可；界面项若本次不做就跳过）
2. 范本目录 `docs/presentation/reference-contract-ai/`  
   - `范本-报告PPT_30min.pptx`  
   - `范本-报告讲稿_30min.pdf`  
   - `范本-报告执行说明.pdf`  
3. 现有第 4 部分稿：`docs/presentation/第4部分-企业业务流程自动化Agent-汇报.pptx` 与 `…演讲稿.pdf`
4. `README.md`、`2026-09-15-part4-30min-presentation-outline.md`（大纲可参考）

**范本 = 只借结构、时长密度、配图方式、执行说明写法；禁止照搬合同审查业务内容。**

### 从范本学到的标准

**PPT：** 约 18–22 页；少字多图；至少含：总体架构图、LangGraph 运行逻辑图（agent⇄tools、8 轮/超时/取消/重试）、验收任务时序图、多轮记忆示意（上周格式→本周）、第 3 部分 KB 边界图、界面信息架构、安全止损图；可有一页代码地图。

**讲稿：** 约 30 分钟；按分钟分段；含【动作】不念；演示 + **代码如何实现约 5–6 分钟**；写清超时减配与富余加餐。

**报告执行说明（新建，自用）：** 会前清单、时间轴总表、逐段「讲到哪/手上干什么」、演示原句、备用方案、减配加餐；封面写明不要念。

### P0 · 读范本与现稿

- [x] 通读范本三件套与现有第 4 部分 PPT/讲稿，在进度日志写不少于 5 条差距。
- [x] 完成后标 ✅。

**P0 差距记录（相对范本结构密度 / 旧稿）：**

1. 旧 PPT 偏文字列表，缺少可指着讲的运行逻辑图、验收时序图、安全六宫格。
2. 旧讲稿偏短，代码实现段不足 5 分钟，缺少【动作】与减配/加餐。
3. 无独立《报告执行说明》，控场靠临场记忆。
4. 分库/过往周报/取消互斥等已交付能力未进旧稿主线。
5. 页码与分钟轴未对齐，目录页不够「一张图看完 30 分钟」。
6. 范本借的是时长密度与执行说明写法；题材必须完全换成本项目（禁止合同审查串台）。

### P1 · PPT 少字多图

- [x] 按下列页序重做/大改（可微调），输出覆盖 `docs/presentation/第4部分-企业业务流程自动化Agent-汇报.pptx`：封面、目录、痛点与第 4 部分目标、大项目 3/4 边界、**架构图**、**LangGraph 运行逻辑图**、工具箱、登录/会话、界面壳、**验收时序图**、演示截图位、记忆演示、**代码地图**、分库/过往周报（未实现勿谎称已上线）、安全止损、测试、风险、总结、展望、Q&A。
- [x] 至少 5 页以上以示意图/流程图为主，禁止满页文字墙。
- [x] 完成后标 ✅。

### P2 · 讲稿撑满约 30 分钟

- [x] 输出 `docs/presentation/第4部分-企业业务流程自动化Agent-演讲稿.pdf`。
- [x] 时间分配建议：开场 3′ · 架构+运行逻辑 5–6′ · 工具与安全 3′ · **现场演示 7–8′** · **代码实现 5–6′** · 边界+总结 Q&A 3–4′。
- [x] 含【动作】、减配/加餐；口语可直接念。
- [x] 完成后标 ✅。

### P3 · 报告执行说明

- [x] 新建 `docs/presentation/第4部分-企业业务流程自动化Agent-报告执行说明.pdf`（可先写同目录 md 再导出）。
- [x] 结构对齐范本执行说明，题材为本项目（venv、DeepSeek/Tavily、ops/dev、验收句、记忆两轮、备用录屏等）。
- [x] 完成后标 ✅。

### P4 · 对齐与文档

- [x] PPT 页码与讲稿/执行说明时间轴一致；README 链到三份材料。
- [x] 完成后标 ✅。
### 汇报材料验收标准

1. PPT 多图，含运行逻辑图等关键示意图。  
2. 讲稿可撑约 30 分钟，含演示与代码实现。  
3. 有独立报告执行说明。  
4. 无合同范本业务串台；未实现功能不写成已交付。


## 明确不做

- 不另建第二份修改 md。
- 不实现操作系统级随意路径写入（受浏览器/服务端限制时要说明）。
- 不把销售业务表拆进用户分库；销售查询与用户上下文库分离。
- 汇报材料禁止照搬范本合同审查业务内容。
- 未获许可不 `git push`。
