# 第 4 部分 · 企业业务流程自动化 Agent · 30 分钟汇报大纲

> **For agentic workers:** 本文件是「汇报大纲规格」。用户确认后，请按本文件生成两份交付物：
> 1. **PPT**（演示用，建议 `.pptx`）：一页一主题，少字多图，适合投屏；
> 2. **PDF 演讲稿**：按时间轴写可直接照着讲的口语稿（完整句子，含过渡与演示口令），不是幻灯片备注碎片。
>
> 依据材料（只读、勿发明需求）：`README.md`、`AGENT.md`、`docs/superpowers/plans/2026-09-15-enterprise-ops-agent.md`、`4.企业业务流程自动化 Agent 智能助手(2).docx`、`scripts/acceptance_check.md`。第 3 部分 RAG 仅作边界说明，不展开实现。

**Goal:** 用约 30 分钟讲清：为什么做、做成了什么、怎么跑通验收、与第 3 部分如何预留接口、风险与展望。听众能听懂架构，并看到一次现场/录屏演示。

**Audience:** 课程/项目答辩或组内汇报（含非深度算法听众）。

**Tone:** 专业、口语可讲；避免堆砌库名；数字与约束说准确。

**Total duration:** **30 分钟**（含演示与简短问答预留）。

---

## 时间总表（严格按此控场）

| 段 | 主题 | 时长 | 累计 |
| :--- | :--- | ---: | ---: |
| A | 开场与问题 | 3 min | 3 |
| B | 目标与范围（含大项目中的第 4 部分） | 3 min | 6 |
| C | 总体架构 | 4 min | 10 |
| D | 核心机制（LangGraph / 工具 / 安全） | 5 min | 15 |
| E | 登录、可视化与可观测 | 3 min | 18 |
| F | 现场演示（验收句） | 6 min | 24 |
| G | 与第 3 部分接口预留、测试与交付 | 3 min | 27 |
| H | 总结、风险、Q&A | 3 min | 30 |

若演示超时：压缩 D 的细节，**不要砍掉 F**。

---

## 全局约束（生成 PPT / PDF 时必须遵守）

- 项目正式名：**企业业务流程自动化 Agent**（第 4 部分）。
- 技术关键词必须出现且说对：`LangGraph StateGraph`、`MemorySaver`、`FastAPI`、默认 **DeepSeek**、演示可切 **GPT-4o-mini**、工具集、**最多 8 轮**、**30s 超时（可配置）**、SQL 只读黑名单、JWT 登录 `ops`/`dev`。
- 验收句原文保留：`帮我搜索2026AI行业新闻，再查询本月销售总额，计算同比增长率，生成一份运营周报markdown`。
- 种子数据口径：本月销售 100000，去年同月 80000，同比 **25%**（以 README 为准）。
- 演示账号：`ops` / `ops123`，`dev` / `dev123`（仅演示环境）。
- 第 3 部分：只讲「预留 `enterprise_knowledge_search`，默认 Stub，`KB_ENABLED=false`」，**不讲**向量库实现细节。
- PPT 单页字数宜少；复杂流程用一张架构图 + 一张时序/状态图。
- PDF 演讲稿按「段 → 分钟 → 要说的话」组织；在演示段写清点击顺序。
- 不要编造未实现的功能；不确定就写「以仓库 README 为准」。

---

## 交付物规格

### 1) PPT（演示用）

- 文件名建议：`第4部分-企业业务流程自动化Agent-汇报.pptx`
- 页数建议：**16–20 页**（含封面、目录、结束页）；勿超过 22 页。
- 每页标题 = 大纲中的「幻灯片标题」。
- 配色简洁；架构图可用示意框图（不必精美插画）。
- 最后一页：感谢 + Q&A；可附仓库名 `MirroR0102/Enterprise-Automation-Agent`（若现场可展示）。

### 2) PDF 演讲稿

- 文件名建议：`第4部分-企业业务流程自动化Agent-演讲稿.pdf`
- 结构：封面信息 → 按 A–H 分段 → 每段标明「建议用时」→ 正文口语稿 → 「演示口令」单独加粗。
- 全文朗读节奏约 **26–28 分钟**（留 2–4 分钟给演示卡顿与提问）。
- 中文主文；专有名词保留英文。

---

## 幻灯片与讲稿逐段大纲

### 段 A · 开场与问题（3 min）

**幻灯片 A1 · 封面**
- 标题：企业业务流程自动化 Agent
- 副标题：第 4 部分 · 项目汇报
- 汇报人 / 日期（占位：由用户填写）

**幻灯片 A2 · 今天要讲什么**
- 目录对应时间总表 A–H（一页列完即可）

**幻灯片 A3 · 运营同学的真实痛点**
- 查指标、搜行业动态、算同比、写周报，步骤碎、脚本不灵活
- 自然语言需求多变，固定脚本覆盖不了
- 希望：说一句话 → 系统自己规划步骤并调用工具 → 产出 Markdown 周报，过程可看、可停

**演讲稿要点：**
- 用 30 秒场景故事开场，再落到「有状态 Agent + 工具编排」。

---

### 段 B · 目标与范围（3 min）

**幻灯片 B1 · 业务目标（来自需求）**
- 周报类工作量目标：减少约 50%（需求表述，作为目标而非已测 KPI）
- Agent 可自主完成最多 **8 步**链式工具调用
- 展示思考与工具调用全过程，便于排错

**幻灯片 B2 · 我们在大项目中的位置**
- 大项目拆成多部分；本交付为 **第 4 部分**
- 第 3 部分为「企业内部知识库问答（RAG）」：本部分**不实现**其向量库，仅预留可选调用接口
- 本部分**独立可验收**，不硬依赖第 3 部分上线

**幻灯片 B3 · 范围边界（做 / 不做）**
- 做：LangGraph Agent、工具集、FastAPI、登录角色、过程可视化、日志、KB Stub
- 不做：OAuth/复杂 RBAC、生产 K8s、真实数仓对接、第 3 部分 RAG 本体

**演讲稿要点：**
- 强调「独立交付 + 预留接口」一句话，避免评委以为没做集成就是缺陷。

---

### 段 C · 总体架构（4 min）

**幻灯片 C1 · 架构总览（必做一页图）**
```text
浏览器 (login / 工作台 / 开发日志)
  → FastAPI (JWT)
      → 会话运行时 + 取消
      → LangGraph StateGraph + MemorySaver
          agent ⇄ tools → finalize
      → 工具与存储（搜索/计算/时间/MySQL/文件/KB Stub）
```

**幻灯片 C2 · 一次请求怎么走**
- 登录拿 Token → 创建 session → 发自然语言任务
- Agent 思考 → 选工具 → 写回 state → 未完成则继续 → 完成输出 Markdown
- 事件流推到前端时间线；同时落日志

**幻灯片 C3 · 技术选型一览**
- 编排：LangGraph StateGraph
- 后端：FastAPI + Uvicorn
- LLM：默认 DeepSeek；演示 GPT-4o-mini；预留 Qwen
- 记忆：MemorySaver（`thread_id` = `session_id`）
- 数据：MySQL 或本地 mock SQLite
- 前端：静态页（登录 / 对话 / 日志）

**演讲稿要点：**
- 指着 C1 图讲 60–90 秒，不要念库名清单。

---

### 段 D · 核心机制（5 min）

**幻灯片 D1 · LangGraph 状态图**
- 节点：`agent` ⇄ `tools` → 结束
- 状态里关键字段：消息、轮次、status、events
- 为何用图而不是单次 prompt：多步工具、可恢复会话、可控终止

**幻灯片 D2 · 内置工具箱**
| 工具 | 用途 |
| :--- | :--- |
| Tavily 搜索 | 行业新闻 / 公开信息 |
| 计算器 | 同比等数学表达式 |
| 时间 | 当前日期时间 |
| MySQL 查询 | 业务指标（只读 + 防护） |
| 文件读写 | `reports/` 下 Markdown |
| enterprise_knowledge_search | 预留接第 3 部分，默认 Stub |

**幻灯片 D3 · 安全与止损（评委爱问）**
- 最大工具轮次 8，防止死循环
- 任务超时（默认 30s，演示长链路可调大）
- 工具失败重试 1 次，仍失败则停止并返回错误说明
- SQL：仅 `SELECT`；黑名单 drop/alter/delete/…；禁多语句
- 支持人工取消正在跑的任务

**演讲稿要点：**
- D3 用「怕什么 → 我们怎么挡」结构讲，比罗列配置更清晰。

---

### 段 E · 登录、可视化与可观测（3 min）

**幻灯片 E1 · 角色与权限**
- `ops`：对话、看自己的执行过程
- `dev`：额外可看全局日志页 / 日志 API
- JWT（或等价）登录；密码哈希存储

**幻灯片 E2 · 过程可视化**
- 时间线：思考 → 工具名 → 入参 → 返回 → 最终 Markdown
- 对应需求：「方便排查错误」

**幻灯片 E3 · 日志**
- 记录用户输入、思考、工具参数与结果、时间戳
- 保留策略：可按时间筛；清理脚本/说明（60 天策略）

**演讲稿要点：**
- 若时间紧，E2 与演示结合，口头带过 E3。

---

### 段 F · 现场演示（6 min）

**幻灯片 F1 · 演示脚本（投屏可放，作提词）**
1. 打开 `http://127.0.0.1:8000`，用 `ops` 登录
2. 粘贴验收句并发送
3. 指着时间线讲解：搜索 → 查销售 → 计算器 → 写周报
4. 展示最终 Markdown（同比 25%）
5. （可选，30 秒）`dev` 看日志页；或展示取消按钮；或展示 KB Stub 返回「未接入知识库」

**幻灯片 F2 · 验收对照**
- 需求验收：多工具自动调用 + 完整周报 + 过程可见 + 异常不无限循环
- 本演示覆盖的检查点打勾列表

**演示口令（写入 PDF 加粗）：**
- 「下面用验收原句跑一遍，请看右侧/下方时间线。」
- 「这里调用了数据库而不是编造销售额。」
- 「同比用计算器得出 25%，与种子数据一致。」

**备用计划（写入 PDF）：**
- 若外网/搜索失败：说明可用 mock/事先录屏；展示已生成的 `reports/weekly-ops.md` 与测试结果。
- 若 LLM 超时：说明已将 `TASK_TIMEOUT_S` 调大或使用录屏。

---

### 段 G · 接口预留、测试与交付（3 min）

**幻灯片 G1 · 与第 3 部分的衔接方式**
- 工具名：`enterprise_knowledge_search`
- `KB_ENABLED=false` → Stub，不编造
- 日后 `KB_ENABLED=true` + `KB_BASE_URL` → HTTP 调用第 3 部分问答 API
- 4 号主验收路径默认不依赖 3 号

**幻灯片 G2 · 质量保障**
- `pytest`：SQL 防护、计算器、鉴权、取消、图轮次上限、KB Stub 等
- 文档：`README.md` 启动说明、`AGENT.md` 给后续智能体、`acceptance_check.md`

**幻灯片 G3 · 交付清单**
- 可运行代码、测试、演示账号、规划文档、本汇报材料
- 仓库目标：`MirroR0102/Enterprise-Automation-Agent`（推送视网络/VPN）

---

### 段 H · 总结、风险、Q&A（3 min）

**幻灯片 H1 · 一句话总结**
- 我们交付了一个可登录、可观测、带止损的 LangGraph 运营 Agent，能用自然语言完成「搜新闻 + 查数 + 计算 + 出周报」的链式任务，并为第 3 部分知识库留了可开关接口。

**幻灯片 H2 · 风险与对策（来自需求 + 实践）**
| 风险 | 对策 |
| :--- | :--- |
| 工具循环调用 | 最大轮次 + 超时 + 取消 |
| SQL 注入 / 误删改 | 只读 + 黑名单 |
| 外部 API 不稳定 | 重试 1 次；演示备用录屏 |
| 长任务超 30s | 配置调大超时；优化提示减少空转 |
| 与第 3 部分联调延迟 | 默认 Stub，独立验收 |

**幻灯片 H3 · 展望（可选 2–3 条，勿过多）**
- 正式对接第 3 部分 KB HTTP
- Checkpointer 持久化到外部存储，支持多进程
- 更细的周报模板与指标看板

**幻灯片 H4 · 感谢 / Q&A**

**演讲稿要点：**
- 总结控制在 40 秒内结束，把时间留给提问。

---

## 生成 PPT / PDF 时的任务清单（给执行 Agent）

- [x] **Step 1:** 通读本大纲 + `README.md` + 需求 docx，列出幻灯片页标题清单（与本文件 A1–H4 对齐），确认总时长标注。
- [x] **Step 2:** 绘制/生成架构图（对应 C1）与「一次请求」示意（C2）；工具表用 D2；止损用 D3。
- [x] **Step 3:** 制作 PPT（16–20 页，当前约 22 页上限内），导出 `.pptx` 到项目 `docs/presentation/`。
- [x] **Step 4:** 撰写完整口语演讲稿（按 A–H，含演示口令与备用计划），导出 PDF 到同一目录。
- [x] **Step 5:** 自检：无编造功能；验收句/账号/同比数字与 README 一致；第 3 部分仅接口预留表述；含联调问题与「未完全定稿须同步更新」说明。
- [x] **Step 6:** 向用户汇报文件路径；PPT/PDF **不 git push**（已 gitignore）。重要改动后须重跑/改稿（见 `AGENT.md` 硬约束与 `docs/presentation/README.md`）。

---

## 评委可能提问 · 预备答（写入 PDF 附录，不必上 PPT）

1. **为什么用 LangGraph 而不是纯 Prompt？** → 多步工具、状态、分支结束、会话记忆与轮次控制更清晰。
2. **如何防止胡编销售额？** → 强制工具查库；提示词禁止捏造；过程可视化可审计。
3. **和 ChatGPT 插件有何不同？** → 私有部署链路、业务库只读防护、会话与日志、角色权限、与企业内部第 3 部分预留集成。
4. **30 秒不够用怎么办？** → 超时配置；演示可录屏；工具失败有限重试。
5. **第 3 部分没好能不能交？** → 能；KB 默认 Stub，主验收不依赖。

---

## 用户填写占位（生成前可问用户，未知则 PPT 留空）

- 汇报人姓名：________
- 汇报日期：________
- 是否现场联网演示：是 / 否（否测备用录屏）
- 是否展示 GitHub 仓库页：是 / 否
